{
    "minEta": 6,
    "minPxCount": 2,
    "skipFitIfExists": "yes",
    "fitPeakShapePV": "no",
    "R_deviation": 10.0,
    "lrsz": 20,
    "edge_bound_cutoff_fac": 2.5,
    "batch_size": 400,
    "peak_fit_method": "midas_lm",
    "_peak_fit_method_about": "Which SR-MIDAS peak-fit routine to use in run_sr_process. Options: 'midas_lm' (DEFAULT: fit SR patches with the real midas_peakfit Levenberg-Marquardt engine, identical pseudo-Voigt model/seeds to MIDAS's no-SR peakfit; needs the midas-peakfit package, falls back to gpu_adam if absent), 'gpu_adam' (fast batched-Adam on GPU, no BG term), 'gpu_midas_style' (batched-Adam on GPU with MIDAS-style BG + moment-init + region-dependent bounds + MIDAS IntegratedIntensity/NrPixels rules), 'midas_style' (CPU per-patch Nelder-Mead with BG, very slow; for methodology verification only).",
    "mods_to_use": {
        "SRx2": {
            "mod_dir": "x1_x2-pst60000-240117_arch3-itrOut",
            "mod_itr": 4975,
            "about": "Takes x1 patch as input and predicts SRx2 patch"
        },
        "SRx4": {
            "mod_dir": "x2pred_x4-pst60000-240117_arch3-itrOut",
            "mod_itr": 999,
            "about": "Takes SRx2 predicted patch as input and predicts SRx4 patch"
        },
        "SRx8": {
            "mod_dir": "x4Pred_x8-pst60000-240117_arch3-itrOut",
            "mod_itr": 999,
            "about": "Takes SRx4 predicted patch as input and predicts SRx8 patch"
        },
        "about": "mod_dir values are directory names within sr_midas/models/cnnsr/pretrained/"
    },
    "spot_find_args": {
        "threshold": 30,
        "patch_size": 20,
        "about": "parameters for finding diffraction spots in a frame"
    },
    "peak_find_args": {
        "min_d": {
            "SRx1": 2, "SRx2": 2, "SRx4": 4, "SRx8": 5,
            "about": "minimum distance (in pixels) between separate peaks in a patch"
        },
        "thresh_rel": {
            "SRx1": 0.2, "SRx2": 0.2, "SRx4": 0.2, "SRx8": 0.1,
            "about": "threshold relative to max intensity within a patch for a peak to be considered"
        },
        "gauss_filter_sigma": {
            "SRx1": 0, "SRx2": 0, "SRx4": 0, "SRx8": 0,
            "about": "sigma of the gaussian filter applied before peak fitting"
        },
        "median_filter_size": {
            "SRx1": 1, "SRx2": 1, "SRx4": 1, "SRx8": 1,
            "about": "size of the median filter applied before peak fitting"
        },
        "peak_crop_size": {
            "SRx1": 2, "SRx2": 8, "SRx4": 10, "SRx8": 20,
            "about": "size of the peak patch extracted around each position for fitting"
        },
        "pvfit_int_thresh": {
            "SRx1": 60, "SRx2": 20, "SRx4": 15, "SRx8": 10,
            "about": "intensity threshold used for pseudo-Voigt fitting"
        }
    },
    "_shift_YZ_pos_about": "Sub-pixel shift corrections that map SR-grid coords to MIDAS pixel-center convention are auto-computed in sr_process.py as -(srfac-1)/(2*srfac) (= -0.25 for SRx2, -0.375 for SRx4, -0.4375 for SRx8). To override, add a 'shift_YZ_pos' block with the same shape as before (per-srfac shiftYpx / shiftZpx)."
}
